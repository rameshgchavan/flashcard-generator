// imported all pages action to combine in one to avoid confliction

import { createFlashcard } from "./createFlashcard";
import { setFlashcards } from "./myFlashcards"
import { setFlashcard, viewTerms } from "./flashcardDetails"

export { createFlashcard, setFlashcards, setFlashcard, viewTerms }